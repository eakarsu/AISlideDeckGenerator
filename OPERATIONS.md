# Operations

Provision least-privilege PostgreSQL, copy `.env.example`, replace credentials and set a random 32+ character JWT secret. Install dependencies explicitly and run `./scripts/migrate.sh`; `./start.sh` does not install, seed, create databases, kill ports or mutate schema.

Monitor render retries/dead letters, expired licenses, failed accessibility/brand/export evaluations and stale versions. Editors must correct blocked decks; publishers must retain channel receipts and disclosure evidence. Remove source/output objects through the storage provider before recording deletion receipts. Direct/generated AI and gap routes remain quarantined.
